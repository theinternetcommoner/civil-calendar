services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5050:5050 # host_port:container_port
    volumes:
      # - source(host):target(container)
      - type: bind
        source: 'C:\civilcal'
        target: /calendar
    restart: always
    command: python app.py

# docker run -d --restart always --name rtc2-1 -v C:\rtc2\instance:/rtc2/instance -p 5001:5001 theinternetcommoner/rtc2:1.0